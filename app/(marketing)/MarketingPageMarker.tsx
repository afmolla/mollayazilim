"use client";

/**
 * `(marketing)/page` segmentinde en az bir client reference olsun diye (Next/Vercel
 * `page_client-reference-manifest.js` bekleyebiliyor).
 */
export function MarketingPageMarker() {
  return null;
}
